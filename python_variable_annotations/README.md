# Python - Variable Annotations

This project introduces type annotations in Python 3, including annotating variables and functions to improve readability and enable static type checking with tools like mypy.
